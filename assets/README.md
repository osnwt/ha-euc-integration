This directory stores repository-level branding assets for publishing and documentation.

- `icon.png` is the square integration icon.
- `logo.png` is the current logo asset and currently matches `icon.png`.

These files are duplicated into `custom_components/euc/brand/` for local integration branding.
